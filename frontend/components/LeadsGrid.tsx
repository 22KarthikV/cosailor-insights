/**
 * LeadsGrid — thin re-export kept for backwards compatibility.
 *
 * New code should import LeadsGridClient directly. This file exists so that
 * any historic imports of LeadsGrid continue to resolve without changes.
 */
export { LeadsGridClient as LeadsGrid } from './LeadsGridClient';
